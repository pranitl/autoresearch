[project]
name = "autoresearch"
version = "0.1.0"
description = "LLM-as-judge optimization loops for CRO copy experiments"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.0",
]
